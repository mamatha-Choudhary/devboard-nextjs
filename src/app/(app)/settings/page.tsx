import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Settings",
  description:
    "Manage your workspace settings, notifications, and preferences.",
};

export default function SettingsPage() {
  return <h1>Settings</h1>;
}
