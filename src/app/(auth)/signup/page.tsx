import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Account",
  description: "Create a new DevBoard workspace account.",
};

export default function SignupPage() {
  return <h1>Signup</h1>;
}
