import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Sign In",
  description: "Sign in to your DevBoard workspace.",
};

export default function LoginPage() {
  return <h1>Login</h1>;
}
